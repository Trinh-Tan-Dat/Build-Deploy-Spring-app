name: Build & Deploy app

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    name: Build & Deploy Spring app
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: 17

      - name: Uni Tests
        run: mvn -B test --file pom.xml

      - name: Build the application
        run: |
          mvn clean
          mvn -B package --file pom.xml

      - name: Build Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./Dockerfile
          push: false
          tag: ${{ secrets.datuits}}/spring-boot-for-beginners:$latest

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.datuits }}
          password: ${{ secrets.dckr_pat_jdjHUO3Dd7Oi8mdnronitcIVodg }}

      - name: Push to Docker Hub
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./Dockerfile
          push: true
          tag: ${{ secrets.datuits}}/spring-boot-for-beginners:$latest